# Copy to .env and fill in. Vite builds it into the page, so none of this is
# secret - it is kept out of the repository only so each copy names its own.

# The OAuth client id of a Google Cloud project with the Drive API enabled,
# whose authorised JavaScript origins include http://localhost:4173,
# http://localhost:5173 and the published site. Without it the trainer has no
# Google Drive sync; everything else works.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# A second client of the same project, of the kind "TVs and Limited Input
# devices", for signing in by a code entered on another device - where the
# browser will not open Google's window. The same project, or the drive shows
# it none of the files the first one made. Without it there is no code button.
VITE_GOOGLE_DEVICE_CLIENT_ID=your-device-client-id.apps.googleusercontent.com
VITE_GOOGLE_DEVICE_CLIENT_SECRET=its-secret
